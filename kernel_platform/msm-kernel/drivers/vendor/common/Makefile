
# Makefile for the common kernel device drivers.
obj-$(CONFIG_POWER_VENDOR_COMMON) += power/
# obj-$(CONFIG_SND_SOC_TFA98XX) += tfa9873/
# obj-y   += sound/
# obj-y += tfa9873/
# obj-$(CONFIG_SND_SMARTPA_AW882XX) += aw882xx/
obj-y += touchscreen_v2/

#for zcard_det
obj-y   += zcard_det/

obj-$(CONFIG_VENDOR_ZTE_DEV_MONITOR_SYSTEM)   += zlog/

obj-y  += fan/

obj-y += aw96205/

obj-y  += nubia_gpio_keys/

obj-y  += nubia_hw_version/

obj-y   += sensor/
